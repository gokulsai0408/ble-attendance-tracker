name: ble_attendance_tracker
description: A BLE based attendance tracking system.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.31.0
  http: ^1.2.2
  provider: ^6.1.1
  fl_chart: ^0.66.0
  percent_indicator: ^4.2.3
  google_fonts: ^6.1.0
  intl: ^0.19.0
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
